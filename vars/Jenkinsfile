@Library("shared-library") _ 
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                welcome("lakhan")
                call("lakhan")
            }
        }
    }
}
